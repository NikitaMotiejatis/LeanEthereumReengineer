[package]
name = "containers"
version = "0.1.0"
edition = "2021"

[lib]
name = "containers"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
# Add SSZ later if you want: ethereum_ssz, ethereum_ssz_derive